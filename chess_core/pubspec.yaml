name: chess_core
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  board_game_core:
    path: ../board_game_core

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

publish_to: none
